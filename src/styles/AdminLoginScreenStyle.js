import { StyleSheet } from 'react-native';

const AdminLoginScreenStyle = StyleSheet.create({
  // Ana container
  container: {
    flex: 1,
    backgroundColor: '#fff',
  },

  // ScrollView inner container
  inner: {
    flexGrow: 1,
    justifyContent: 'center',
    paddingVertical: 20,
    paddingHorizontal: 16,
  },
});

export default AdminLoginScreenStyle;