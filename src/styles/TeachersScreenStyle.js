import { StyleSheet } from 'react-native';
import { useTranslation } from 'react-i18next';

const TeachersScreenStyle = StyleSheet.create({
  // Ana container
  container: {
    flex: 1,
    backgroundColor: '#f5f5f5',
  },
});

export default TeachersScreenStyle;