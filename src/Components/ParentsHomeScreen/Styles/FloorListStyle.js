import { StyleSheet } from 'react-native';
import { useTranslation } from 'react-i18next';

const FloorListStyle = StyleSheet.create({
  // FlatList container styling
  list: {
    paddingVertical: 10,
    paddingBottom: 20,
    flexGrow: 1,
  },
});

export default FloorListStyle;